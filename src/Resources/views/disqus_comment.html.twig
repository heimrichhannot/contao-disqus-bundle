<div id="disqus_thread"></div>
<script>
    {% if disqus_identifier is not null or disqus_pageUrl is not null %}
    var disqus_config = function () {
        {% if disqus_pageUrl is not null %}
            this.page.url = '{{ disqus_pageUrl }}';
        {% endif %}
        {% if disqus_identifier is not null %}
            this.page.identifier = '{{ disqus_identifier }}';
        {% endif %}
    };
    {% endif %}

    (function() {  // REQUIRED CONFIGURATION VARIABLE: EDIT THE SHORTNAME BELOW
        var d = document, s = d.createElement('script');

        s.src = '//{{ disqus_shortname }}.disqus.com/embed.js';  // IMPORTANT: Replace EXAMPLE with your forum shortname!

        s.setAttribute('data-timestamp', +new Date());
        (d.head || d.body).appendChild(s);
    })();
</script>
<noscript>Please enable JavaScript to view the <a href="https://disqus.com/?ref_noscript" rel="nofollow">comments powered by Disqus.</a></noscript>